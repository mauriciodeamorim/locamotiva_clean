<!index de resultado>
<div>
    <div class='data' style="float:left">
        <div style="text-align:center;float:none;"><%= @last_competition.data.strftime('%d') %></div>
        <div style="text-align:center;"><%= @last_competition.data.strftime('%b') %></div>
    </div>
    <div class="prova" style="float:left"><%=h @last_competition.nome %></div>
    <div class="prova" style="float:left"><%=h @last_competition.distancia %></div>
</div>
<div>
    Only a simple test <%= "#{@meu_nome}" %>
    <%=h @meu_nome %>
    <%= @meu_nome %>
    <% @meu_nome %>
    <%= "#{@meu_nome}" %>
</div>
<table style="clear:both; padding-top:20px;">
  <tr>
    <td>Numero de peito</td>
    <td>Atleta</td>
    <td>Tempo oficial</td>
    <td>Tempo líquido</td>
    <td>Classificação</td>
  </tr>

<% for r in @results %>
  <tr>
    <td><%=h r.nome %></td>
    <td><%=h r.atleta.nome %></td>
    <td><%=h r.tempoOficial.strftime("%H:%M:%S") %></td>
    <td><%=h r.tempoLiquido.strftime("%H:%M:%S") %></td>
    <td><%=h r.classGeral %></td>
  </tr>
<% end %>
</table>

<!--only test -->
    <table>
      <tr>
        <td>Id</td>
        <td>Nome</td>
        <td>Etapa</td>
        <td>Data</td>
        <td>Local</td>
        <td>Url</td>
        <td>Distância</td>
      </tr>

    <% for competition in @all_competitions %>
      <tr>
        <td><%=h competition.id %></td>
        <td> --- </td>
        <td><%= link_to competition.nome, "resultados/show/#{competition.id}" %></td>
        <td><%=h competition.etapa %></td>
        <td><%=h competition.data %></td>
        <td><%=h competition.local %></td>
        <td><%=h competition.url %></td>
        <td><%=h competition.distancia %></td>
      </tr>
    <% end %>
    </table>

